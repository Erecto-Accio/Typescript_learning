let prices: number[] = [100, 75, 6, 56, 6];
console.log(prices);

let fruits: string[] = ["Apple", "pineapple", "Jackfruit", "Guava"];

console.log(fruits);

//random array
let randomArray: any[] = ["hello", 1, 2, 3, false];
console.log(randomArray);

// empty array
let emptyArray: boolean[] = [];
console.log(emptyArray);

// union array
let names = ["peter", 1];
console.log(names);

let array: (string | boolean)[] = ["apple", true, "orange", false];
console.log(array);
